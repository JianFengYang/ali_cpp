main(read store)
struct Record:word freq

read:Dic reserve
	read() ifs<->filename
	getline->line<->iss->word
	isWord->processWord
	sort
	
store:ofs<->filename
	ofs<-word&freq
	
isWord:word[idx]->isdigit
processWord:_dict[idx]._freq++

vector<Record> _dict;
